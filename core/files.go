package core

/*type Host struct {
	files	map[string]*files
	stream	baluba.BalubaService_UploadServer
	chunck	chan *baluba.Chunk
}

func NewHost(name string, stream baluba.BalubaService_UploadServer) (host *Host) {
	host.files = make(map[string]*files)
	host.stream = stream
	host.chunck = make(chan *baluba.Chunk)

	return
}

func (h *Host) CreateFile(chunk *baluba.Chunk) (err error) {
	return
}*/
